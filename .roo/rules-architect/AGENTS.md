# Project Architecture Rules (Non-Obvious Only)
- Async PostgreSQL database access
- Database models and schemas separated
- API endpoints hardcode user authentication
- Date handling in API needs refactoring
- Database migrations run at app startup